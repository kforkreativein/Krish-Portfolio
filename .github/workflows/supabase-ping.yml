name: Ping Supabase to Keep Alive (DISABLED — app migrated to local JSON CMS)

on:
  workflow_dispatch:

jobs:
  ping-supabase:
    runs-on: ubuntu-latest
    
    steps:
      - name: No-op — Supabase no longer used
        run: echo "This workflow is disabled. The app no longer uses Supabase."
